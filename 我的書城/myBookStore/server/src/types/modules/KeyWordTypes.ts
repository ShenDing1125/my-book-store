/**
 * @brief
 *  keyWord 的宣告類型
 * @data
 *  2023/3
 */
export interface KeyWordTypes {
  keyWordId: number;
  keyWord: string;
}

export enum KeyWordList {
  TABLE_NAME = "keyWord",
  KEY_WORD_ID = "keyWordId",
  KEY_WORD = "keyWord",
}
